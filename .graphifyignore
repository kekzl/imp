# graphify corpus excludes — keep the graph focused on living code + current docs.
# NOTE: once this file exists, graphify stops falling back to the root .gitignore
# for THIS directory, so the build/artifact excludes below are re-stated here to
# stop build/_deps/ (vendored CUTLASS/gtest/json source) from flooding the graph.

# --- graphify-specific: focus excludes ---
# Stale/refuted/superseded historical docs (174k words, 2.6x current-doc volume):
docs/archive/
# Vendored third-party single-header libs (stb_image et al.) — not imp's architecture:
third_party/
# Generated roofline bench-run artifacts: 21k+ JSON-key nodes (83% of the graph),
# the source of the per_kernel / phase_time_ns god-nodes. Kept in git for the
# regress-trend pipeline, but worthless as graph structure.
tools/roofline/history/
# Agent/harness config + skill docs — not imp's architecture. settings.json JSON
# keys (allow/PreToolUse) were mis-extracted as code nodes and became misleading
# high-betweenness bridge-nodes in the report.
.claude/

# --- carried over from root .gitignore (build outputs + vendored deps) ---
build/
build-*/
build-docker/
cmake-build-*/
out/
bringup_artifacts/
mtp_retest/
models/
weights/
checkpoints/
.cache/
.vscode/
.idea/
compile_commands.json
