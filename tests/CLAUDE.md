<!--
layer: L3
audience: agents
verified: 2026-09-13
commit: 81d22eff
-->

# tests - lanes, and which one gates

Most GTest macros run in no CI lane: they need a GPU and CI has none. `make check-test-lanes` prints and pins the split (macros, not executions); `guard_unit_skips` fails the lane on any runtime skip.

## Invariants

- The CI lane is `ctest -L unit` = `make dev-test`: test-core + test-text + the `_unit_e2e_filter` subset of test-e2e. `make test-unit` is not that lane.
- New CPU tests go to test-core, into its explicit source list in `CMakeLists.txt` (`imp_add_test_module`).
- The unit lane has no skips. A test that needs a GPU or a model file lives in a GPU binary: `test-kv` (`test_kv_cache_gpu.cpp`) or `test-e2e` outside `_unit_e2e_filter` (`test_real_checkpoints.cpp`, tokenizer parity). Bookkeeping tests use `KVCache::for_accounting()`; config-only tests read `tests/fixtures/` (`IMP_TEST_FIXTURES_DIR`).
- A green test counts only after mutation validation: break the covered code, confirm the test fails.
- A test whose input cannot reach the defect is worthless even when correct.

## Entry points

- `tests/test_*.cpp`: the CPU lane
- `tests/api/`: HTTP battery, runs against the mock and the real server
- `tests/refs/`: reference vectors; `chat_template_goldens.h` is generated by `gen_chat_goldens.py` (`make chat-goldens`), never hand-edited
- `tools/mutation/`: mutation harness (`run.py`, `catalogue.json`)

## Test

```
make test-gpu        # the GPU suite; started wrong it silently skips most
```

## Pitfalls

- `--gtest_filter` on a `TEST_P`/`TYPED_TEST` suite without wildcards matches 0 tests and reports `PASSED`: `*DetEvalE2ETest*`, not `DetEvalE2ETest.*` (a CPU-lane guard asserts this).
- A surviving mutant is a test hole only if it is not equivalent.
- A timed-out mutation run leaves the mutation in the tree: back the file up to the scratchpad first, never `git checkout` to undo it.
- Test-model env vars pointing at a wrong path skip whole batteries silently.
- Chat-template tests compare the whole rendered prompt (goldens), not a grepped marker (#1572).
- The template family comes from `ChatTemplate::detect_family()`, not the model name: Nemotron-3-Nano and Phi-4-reasoning both ship ChatML.

## Do not touch

`tests/refs/*` without regenerating them deliberately.

## See also

[`docs/audit/TEST_INVENTORY.md`](../docs/audit/TEST_INVENTORY.md) (2026-08-08 record; live counts: `make check-test-lanes`), [`docs/audit/MUTATION_BASELINE.md`](../docs/audit/MUTATION_BASELINE.md). Skill `building-and-testing`.
