<!--
layer: L1
audience: operators
verified: 2026-09-22
commit: 9cbb8004
-->

# Limitations

What imp does not do, does badly, or does without a test behind it. Absences by decision:
[`DESIGN_DECISIONS.md`](DESIGN_DECISIONS.md).

## Five things to weigh first

1. **One GPU, one chip**: `sm_120a` only, no multi-GPU, no tensor parallelism, no other vendor or generation ([`internals/ARCHITECTURE.md`](internals/ARCHITECTURE.md)).
2. **One model resident at a time**: 32 GB fits one; a second model means a swap, paid by the requesting call.
3. **No GPU in CI**: every kernel-level correctness and perf check runs on the maintainer's machine before a push.
4. **Decode measurements move several percent between sessions** on this host; one number is one sample of a distribution ([`PERF.md`](PERF.md)).
5. **Single-author project**: no support rotation, no SLO, no security response process.

## Untested code paths

Every yellow status in [`FEATURES.md`](FEATURES.md); all tracked under #1680 unless noted.

| limitation | affected | workaround | issue |
|---|---|---|---|
| Loads with no dedicated gate | Llama-4 | manual smoke test only | #1680 |
| Type exists, nothing exercises it | FP8 E5M2 KV / weights | none | #1680 |
| Aliases onto the LLaMA path, no checkpoint of its own in a gate | Phi-4 | none | #1680 |
| Shares the Qwen3-VL tower; `make test-vision` runs gemma-3-4b-vl and Qwen3-VL-4B-Instruct only | Qwen3.6-35B-A3B vision | none | #1680 |
| Dequant paths with no gate reading such a checkpoint (Q4_0, Q8_0, Q4_K, Q5_K, Q6_K do have one) | GGUF Q4_1, Q5_0, Q5_1, Q2_K, Q3_K, Q8_K | none | #1680 |
| **Q8_1** (GGUF wire type 9, llama.cpp's activation format) has no weight path at all | GGUF loader | refused at parse | - |
| Cross-check vs llama.cpp is opt-in behind `COMPARE_URL=`; default `make test-rerank` skips it | `/v1/rerank` | set `COMPARE_URL=` | #1680 |
| Implemented, ungated | `/admin/suspend`, `/admin/resume`, `server.model_swap` | none | #1680 |
| SSE frames, usage accounting, `finish_reason`, tool-call streaming: CI's `Real API contract (model-less)` job deselects every token-producing test, no GPU runner | server generation contract | run `make test-server` on a carded machine | #1600, #1559 |
| Perf gate benches `imp-cli`, which never enters the SSE writer, tool-argument filter or per-chunk JSON serialisation | `tools/imp-server/` per-request cost | none; needs a server-side benchmark harness | #1685 |
| One tree vs a pin measured weeks earlier; host moves 4-6 % between sessions; only recorded catch is -36 %, a real -7.3 % shipped at +0.33 % | perf regressions under 8 % | `make verify-ab` (paired, alternating vs `origin/main`); measures only the pin's shape (Qwen3-8B Q8_0, pp512/tg128) | [`internals/BENCHMARKING.md`](internals/BENCHMARKING.md) |
| CI has no GPU runner; `roofline.yml` re-parses committed history and fires on `tools/roofline/**` edits only | kernel roofline regressions | `make roofline-measure` manually (pre-push hook reminds, cannot measure) | - |

## Gates that do not exist

| limitation | affected | workaround | issue |
|---|---|---|---|
| No correctness gate against a reference implementation: no KL divergence vs an fp16/bf16 forward, no perplexity-drift baseline, no tool-schema conformance rate | quantisation quality | `scripts/validate_safetensors.py:11-14` lists the phases it cannot run (no BF16 checkpoint on disk, imp consumes pre-quantised weights); `make test-niah` exists (`Makefile:315`) but no workflow invokes it (needs `check-gpu`); quality judged by degeneration smoke prompts and by hand | #1571 |
| No soak or endurance test | production stability | largest driven load is 10 concurrent requests (`tests/api/test_concurrency.py:37`); three shipped comments describe what a soak would assert: `tools/imp-server/metrics_memory.cpp:79`, `tests/test_memory_backend.cpp:236`, `src/memory/alloc_interpose.cpp:136`; leaks, KV-pool fragmentation and handle exhaustion surface in production | #1642 |
| No device-memory checker runs against the kernels in any automated lane | memory safety | CI `test` job's memcheck step sits behind `HAS_GPU_RUNNER` (unset) and is `continue-on-error`; `make sanitize` fails to initialise on WSL2 (verified 2026-06-04); `scripts/verify.sh` never calls it; `make asan` and the `Sanitizers` job cover host code only | AUDIT_arch_2026 F2-7 |

## Known-bad and known-limited behaviour

| limitation | affected | workaround | issue |
|---|---|---|---|
| One LoRA adapter at a time: engine-global (executor pointer, decode graphs) | LoRA serving | batching worker serializes by adapter (a request naming a different one waits for in-flight requests, then switches, never batched with them); prefix cache is keyed by adapter, so a shared system prompt prefills once per adapter; C API `imp_lora_set` has no such guard, swap between requests only | AUDIT_arch_2026 E-1 |
| GGUF batched decode rows read the 4-bit overlay with 4-bit activations | batched decode (2-32 rows) on a GGUF source | small-M NVFP4 GEMM runs on the NVFP4 decode overlay (`smallm_weight_`, `src/exec/executor_gemm_smallm.cu`); single-stream decode reads the same overlay with FP16 activations, prompt rows keep the full-precision dequant route; batched output not byte-identical to single-stream at temperature 0; numbers in [`PERF.md`](PERF.md) "Serving KPIs" | #1897, #1766 |
| `server.green_contexts=true` gives no green contexts on this chip | sm_120 decode scheduling | `cudaDevResourceGenerateDesc` fails for the decode partition; fallback is priority streams with distinct memSyncDomains, `has_green_contexts()` stays false; dynamic SM reconfiguration in `step_schedule()` is gated on the flag and never runs [PROV: date=2026-08-22 hw=RTX5090, 170 SMs, 80/20 split, 99/1 retry] | reconfigure race #1656 is real code, unreachable on sm_120 |
| Remote `image_url` fetching is vulnerable to DNS rebinding when on (off by default) | `--allow-remote-images` | check and connection are two separate resolutions, so a name whose records change in between reaches a private address; fix needs a connect-time callback httplib does not expose; treat the flag as "this server's network is trusted" | - |
| VRAM planner's weight-cache reserve is an estimate with a floor; no retry when wrong | server startup on tight VRAM | overcommit ends in `imp_context_create` aborting, not a smaller KV pool; #1631 fixed unstartable-at-defaults on Qwen3-8B-Q8_0 by raising the reserve to projection plus floor (projection alone plans 9977 KV blocks and OOMs, the working arm plans 7079, a 500 MiB edge); a model inside that edge can still fail to start | #1631 |
| Measured library reserve survives only if the cache path outlives the process | `docker run --rm` servers | `vram.library_reserve_cache` defaults inside the container; a `--rm` server re-measures every start and plans with the 3900 MiB constant, wrong in both directions [PROV: date=2026-09-04, after #1899: 763 MiB Qwen3-8B-Q8_0, 1366 Qwen3-14B-Q6_K, 3260 Qwen3.8-27B-NVFP4]; mount the named volume `-v imp-cache:/home/imp/.cache/imp`, or the constant over-reserves KV by ~640 MiB; a bind mount must be writable by container uid 1001 (`imp`): Docker creates a missing host path as root, a host user's directory is uid 1000, both log `library reserve: could not write` and re-measure every start | #1899 (fixed: cold Qwen3-8B-Q8_0 now 289.6/1462.2/4568.5 output tok/s at c=1/8/32, was 124.8/514.1/2375.3, with 1190 MiB free after init [PROV: date=2026-09-04 image=ghcr.io/kekzl/imp:0.37.0]) |
| JSON Schema: assertion keywords imp cannot enforce return a `400`, not a weaker grammar | `minimum`, `maximum`, `exclusiveMinimum`, `exclusiveMaximum`, `multipleOf`, `allOf`, `not`, `uniqueItems`, `patternProperties`, `propertyNames`, `prefixItems`, `contains`, `minContains`, `maxContains`, `minProperties`, `maxProperties`, `dependentRequired`, `dependentSchemas`, `if`/`then`/`else` | pure annotations (`format`, `title`, `description`, `examples`, `default`, `$schema`) stay ignored per Draft 2020-12; `const` is enforced as a one-member enum | #1567 (previously accepted and dropped: an unbounded grammar at HTTP 200) |
| JSON Schema: `enum`/`const` members must be strings | any non-string enum | FSM emits enums as quoted string content (`src/compute/schema_constrain.cu:288`); `{"enum":[1,2]}` is a `400` | #1564 (previously constrained the model to the empty string) |
| JSON Schema: `additionalProperties` as a schema object reads as `true` | schemas from Pydantic `Dict[str, T]` | boolean form enforced; object form is parsed and its constraint dropped: weaker than asked, not wrong | #1564 (previously truncated the schema at that key: everything after it, `properties` included, discarded, request downgraded to `json_object`) |
| A `pattern` the regex engine cannot compile is not enforced; the request still returns 200 | JSON Schema `pattern` | `compile_patterns()` warns and leaves the node unconstrained (`src/compute/json_schema.cpp:624`); a top-level `regex` constraint IS refused at admission, this path has no such screen | - |
| Calibrated KV-cache scales in checkpoints are not read | checkpoints carrying `*.self_attn.{k_proj.k_scale,v_proj.v_scale}` (96 tensors on Qwen3-Coder-30B-A3B-FP4, 12 on NVIDIA-Nemotron-3.5-Lightning-30B) | no consumer in the tree; FP8 KV path derives its own scales; quality effect of adopting them unmeasured | #1497 (among 270 false "unrecognised weight name" warnings) |
| INT4 KV cache produces empty output | gpt-oss | sink term is correct and unit-tested against a sink-aware reference; 4 bits per value on a 64-wide head is not enough | falls back to FP16 |
| Host-offloaded NVFP4 MoE experts are slow, and nothing gates them | Qwen3-30B-A3B-NVFP4, all 48 layers host-offloaded | 23.3 tok/s against 384.0 resident [PROV: commit=8a7bd8c date=2026-08-13 hw=RTX5090 model=Qwen3-30B-A3B-NVFP4-Modelopt quant=NVFP4 cuda=13.3 path=nvfp4-moe-host-offload cmd=`imp-cli --max-tokens 220 --temperature 0 --set moe.force_host_experts=48` n=1 note=single greedy run per arm, resident arm is the same command without the --set; cold cache, short prompt, not a benchmark figure]; only manual checks exercise the path; prefill takes the serial per-expert fallback; a placement the expert cache cannot hold is refused at load | #1403 |
| Batched and solo decode disagree on roughly one greedy token in nine | batch-32 vs batch-1 serving | teacher-forced Qwen3-14B-NVFP4, M=1 vs M=32: 7 of 64 argmax tokens differ, mean NLL 3.4403 -> 3.4889; `gemm.nvfp4_smallm=false` does not shrink it (batch shape is the term, not activation format); pin batch composition or serve at batch 1 | [`determinism.md`](determinism.md), [`PERF.md`](PERF.md) "Batch invariance" |
| Identical seeds can diverge in default mode | any model, default config | sources: cuBLASLt timing-based algo selection, sampler cross-block FP reductions (top-k, `typical_p`), FP32-compute MoE scatter fallback (default F16 fused scatter has no atomics); full list and the opt-in mode | [`determinism.md`](determinism.md) |
| Speculative decoding is not universally profitable per model | Nemotron-3.5 vs Qwen3.8-27B-NVFP4 | economics are per-model: see [`DESIGN_DECISIONS.md`](DESIGN_DECISIONS.md); on Qwen3.8-27B-NVFP4 auto mode (`mtp_k=-1`, default since 2026-08-29) engages `mtp_k=2`+`ngram=false` on single-stream requests whose checkpoint ships a head, 95.8 -> 141.6 tok/s on a thinking prompt [PROV: commit=3ce0c326+mtp-fixes date=2026-08-27]; declines under concurrency (head binds one request, costs batch slots) or `runtime.deterministic` | full ledger: [`archive/limitations_detail_2026.md`](archive/limitations_detail_2026.md#speculative-decoding-economics) |
| MTP release status and cached-verify fidelity differ by model class | dense GDN hybrid (Qwen3.8-27B-NVFP4): released, 1/1033 verify replays disagree with eager (0.10 %), `mtp_k=1` measured +21.3 % decode; MoE+GDN hybrid (Qwen3.6-35B-A3B-NVFP4): released, unmeasured for throughput, 2/1013 disagree (0.20 %); MoE+Mamba2 hybrid `nemotron_h` (Nemotron-3.5-Lightning-30B-A3B-NVFP4): **not released**, 176/1318 (13.4 %) disagree | a checkpoint shipping a head reports `mtp_head_available` on `GET /health`; head costs 0.79 GiB VRAM | #1537; Mamba2-hybrid root cause: [`archive/limitations_detail_2026.md`](archive/limitations_detail_2026.md#nemotron-mtp-defect) |
| Speculation is off for most real requests, by easy-to-miss rules | requests using non-greedy sampling, a think budget, or one of six request fields | requires greedy sampling (`temperature: 0` or `top_k: 1`); default `think_budget=0.5` disables it inside the think block on reasoning models out of the box; penalties are not a blocker at the default `repeat_last_n: 0`; `logprobs`, `json_mode`, `json_schema`, `regex_pattern`, `grammar`, `tool_constraint_tools` disable it outright (`src/runtime/engine_spec_ngram.cpp:143-144`, the verify chunk replicates no FSM masks) | `logprobs` cannot observe what speculation does |
| The same request can produce different output on its second pass through one process | greedy requests sharing a server process | two cross-request carriers: the n-gram drafter's corpus, and the MTP head's own KV cache (resumed over the longest common prefix); three prompts run twice in one server, the first differed on the second pass, two fresh processes running the same three prompts were byte-identical; greedy path only, `imp_spec_drafted_total` does not move on a `temperature: 0.8` request with the drafter enabled, a sampled request diverging between identical runs is a different mechanism (without `runtime.deterministic_gemm` the forward is not bit-reproducible, a last-bit logit difference moves a near-tied sample) | golden tests must pin `speculative.ngram` and `speculative.mtp_k` |
| On a recurrent model, hybrid prefix caching carries prior-traffic-dependent output into a sampled request | GDN models with `server.prefix_cache=true` | GDN state is cumulative, so KV-block reuse alone cannot skip prefill; imp saves state at block boundaries (`RecurrentSnapshotStore`, `server.recurrent_snapshot_host_mb` for the host tier) and restores it on a prefix hit (`src/runtime/engine_sampling_stop.cpp:338`); every chat request shares the same template header, so the first block matches across unrelated prompts; measured (Qwen3.8-27B-NVFP4, temperature 0.8, fixed seed): 12 identical requests give 1 distinct answer fresh, 1 with prefix caching on, **3 distinct when 12 unrelated requests precede them** (characterised with `speculative.ngram=false`; the flag also selects the decode-loop burst bound, `speculative.miss_burst` vs `runtime.decode_burst`, so the two settings are different decode configurations even without drafting) | `server.recurrent_snapshot_mb=0` returns it to 1/12; tokens really match, disabling the store also disables hybrid prefix caching |
| MTP speculation can truncate answers on the affected model | Qwen3.8-27B-NVFP4, `speculative.mtp_k=1` | 2/6 prompts end after ~40 tokens with a re-statement of the question [PROV: date=2026-08-19]; root cause: the verify chunk's bonus token predicts end-of-turn where single-token decode keeps writing; four investigation passes (kernel parity, chunk routing, fused SwiGLU, a stop-decision guard) none reach 0/6; `speculative.mtp_k` stays 0 by default | full investigation: [`archive/limitations_detail_2026.md`](archive/limitations_detail_2026.md#mtp-truncation) |
| Speculative decoding does not reproduce non-speculative greedy output on a GDN hybrid, and cannot by construction | Qwen3.8-27B-NVFP4, `mtp_k=2` | every emitted token in a speculative arm comes out of the multi-row verify chunk, none out of single-token decode; structural, not a kernel defect; all three test prompts diverge (first differing byte 79/332/243); five decode-side hypotheses ruled out by measurement | full investigation: [`archive/limitations_detail_2026.md`](archive/limitations_detail_2026.md#mtp-vs-greedy-divergence) |
| A speculative arm is not byte-stable across processes at temperature 0 | `mtp_k=1`/`mtp_k=2` | `mtp_k=2`: 8/9 processes agree byte for byte on all three prompts, the ninth differs on prompt 3; `mtp_k=1` (coarser): two processes gave 471 vs 1213 bytes on prompt 1; the no-speculation arm is byte-stable across all 11 of its processes | use `speculative.mtp_k=0` + `speculative.ngram=false` for a reproducible arm; details: [`archive/limitations_detail_2026.md`](archive/limitations_detail_2026.md#mtp-cross-process-stability) |
| MTP head accepts 75.0 % of its first draft | Qwen3.8-27B-NVFP4, `mtp_k=1` | 74.8 %/75.2 % over 2 rounds (sigma 1.2, >1200 drafts/cell) [PROV: commit=37cd1543 date=2026-08-17]; six explanations for the gap to published figures ruled out by measurement | details: [`archive/limitations_detail_2026.md`](archive/limitations_detail_2026.md#mtp-acceptance-rate) |
| `imp-cli --prompt` prints only about 10 tokens | byte-level A/B comparisons | go through the server's JSON, not `imp-cli` stdout | - |
| Prefill graph capture is disabled per model when one NVFP4 weight exceeds the dequant-workspace cap | affected NVFP4 checkpoints | decode graphs unaffected | - |
| No video input | video prompts | images work; video needs a decoder this tree does not vendor | - |
| No vision architecture registry | InternVL, Pixtral | tower allowlist covers Qwen3-VL-shaped encoders and Gemma; each needs a port | - |

## Model-specific blockers

| limitation | affected | workaround | issue |
|---|---|---|---|
| Audio is blocked on weights, not on work | Gemma-4 | declares the modality, imp says so at load; Gemma-4-12B-NVFP4 ships exactly one audio tensor (`model.embed_audio.embedding_projection.weight`, BF16 [3840, 640], 4.69 MiB), the bridge into the LM, nothing that produces those 640 dims from a waveform; `audio_config` describes no encoder either (`architectures: null`, no layer or head count, no mel parameters, only `audio_embed_dim`, `rms_norm_eps` and `PretrainedConfig` defaults); the same export carries 10 `model.embed_vision.*` tensors and zero `model.vision_tower.*` (vs 355 tower tensors in Gemma-4-26B-A4B-it-NVFP4), so this quantisation dropped both towers; needs a checkpoint carrying the audio tower, the way `--mmproj` supplies the vision one; imp downloads nothing itself (`scripts/stage-model.sh`) | roadmap Open 8, #1929 |
| Qwen3.5-27B MXFP4 blocked on a checkpoint imp can decode | Qwen3.5-27B | no MXFP4 SafeTensors decode path exists outside gpt-oss; not a bug | - |
| Gemma-4 on the FP8-KV quality gate: baseline perplexity on the gate corpus is broken | Gemma-4-it | scores text only inside its own turn framing; raw prose after BOS reads 19312 (imp) / 20066 (llama.cpp, same GGUF); the same prose plus its turn framing (below) reads 18.16 (UD-Q4_K_M), 26.06 (NVFP4 export), 26.99 (imp-quantize NVFP4), 704 tokens; stays FP16 KV unless opted in; gate corpus `tools/analysis/ppl_corpus_gemma4_turn.txt` | #2044 |
| Declares no FP8 KV hint | Qwen3.6-35B, Qwen3.5 | FP16 KV by default | - |
| Quantised KV is a default only for QWEN35, and it is a trade | Qwen3.8-27B-NVFP4, Qwen3.5-4B-mxfp4 | `kv_cache.dtype=auto` resolves to NVFP4 for that family: +0.29..0.35 % PPL (Qwen3.8-27B), +0.15..0.18 % (Qwen3.5-4B mxfp4), for `max_model_len` 48512 -> 131072 tokens on a 32 GB card [PROV: commit=982cd43 date=2026-08-24]; QWEN36_MOE/QWEN35_MOE excluded (FP8 KV already costs +1.47 % PPL there, NVFP4 KV unmeasured); opt out `kv_cache.dtype=fp16`, opt in elsewhere `=nvfp4` | - |
| GDN-hybrid concurrency fixed 2026-08-24; table is the OLD behaviour, kept because the fix is one release old | GDN models, concurrent decode | before: one sequence per GDN decode step, 32 streams delivered 81.5 tok/s aggregate vs 1427 for a dense model; `runtime.gdn_batched_decode` (default on) batches them: **474.9 tok/s aggregate at 32-way, 5.8x** [PROV: commit=ce77a94 date=2026-08-24 model=Qwen3.8-27B-NVFP4]; `runtime.gdn_batched_decode=false` for the old rotation | - |
| The FP8 release is a quantization source, not a servable checkpoint | Qwen3.8-27B-FP8 | `imp-quantize` reads its per-layer `layers-N.safetensors` layout; nothing serves it: 25.87 GiB weights vs ~27 GiB usable VRAM before KV pool and recurrent state, and sm_120 has no FP8 GEMM; quantize to NVFP4 and serve that; dequant to FP16 at load is the 51.75 GiB BF16 footprint, also not an option | - |

Gemma-4 turn framing used for the gate corpus above:

```
<|turn>user\n...<turn|>\n<|turn>model\n<|channel>thought\n<channel|>
```

## Operational sharp edges

| limitation | affected | workaround | issue |
|---|---|---|---|
| A successful `cudaMalloc` proves nothing about free VRAM on WSL2 | any allocation on WSL2/WDDM | driver oversubscribes into host memory and returns success; 28 GiB allocates with 22.6 GiB reported free; the tell is bandwidth, ~1530 vs ~237 GB/s (6.5x throughput cliff); the KV pool measures itself at load (`kv_pool_bandwidth_gbps` on `/health` and `/metrics`, WARN below 500 GB/s); weights and other caches are not probed, a spill there still shows only as the cliff | [`internals/MEMORY.md`](internals/MEMORY.md) |
| Free VRAM only ever decreases within a process on WSL2/WDDM | any long-running process | anything sized from `cudaMemGetInfo` reads a moving floor, however cleanly CUDA released it | - |
| No `/health` field separates a server started beside another process from a healthy one | multi-tenant WSL2 hosts | does not fail: gets a smaller KV pool and reports it as its own ceiling, 23339 blocks idle against 17406 beside a neighbour holding 23.4 GiB, both `ok`; plan and install-time baseline both read `cudaMemGetInfo`, the same source as the defect; the one signal is the weight-upload delta against the checkpoint on disk, which warns at load | [`internals/MEMORY.md`](internals/MEMORY.md) B8 |
| A prefix-cache hit is not token-identical to a fresh prefill | greedy plus prefix caching | different chunk split, different accumulation order; Qwen3-4B-Q8_0 via the C API: fresh picks token 55486 (logit 38.242889) over 279 (38.081467, gap 0.161), cached returns the same two reversed (38.253368 vs 38.188511), shift 0.172 larger than the candidate gap; `tests/test_prefix_cache_e2e.cpp` asserts a common prefix, not equality | #1314; details: [`determinism.md`](determinism.md) |
| `kv_cache.swa_snapshot_mb` set below one snapshot silently disables prefix caching, worse than zero | SWA models | set it to at least one snapshot size, or to zero | warns since #1092 |
