# Qwen3.8-27B — measured inventory and remaining-work plan

Phase 0 of the "first-class Qwen3.8-27B" brief. Every number below is read off
the staged checkpoints and off runs against `imp:test` (built from `72c642db`,
2026-08-24), not from recollection or from `docs/MODELS.md`.

**The headline finding: the model is already served end to end.** It loads,
generates coherent text, answers about images, and speculates with its own MTP
head. The brief was written as a from-scratch port; it is not one. What follows
separates what already exists from the four gaps that are real.

## The checkpoint

`~/models/Qwen3.8-27B` — 1199 tensors, BF16, config field-identical to
the brief's architecture contract (verified key by key against `config.json`).

| group | tensors | params | BF16 | FP8 | NVFP4 (4.5 b) |
|---|---:|---:|---:|---:|---:|
| MLP (64 L) | 192 | 17,112,760,320 | 31.88 GiB | 15.94 | 8.96 |
| GDN `linear_attn` (48 L) | 432 | 5,562,051,072 | 10.36 | 5.18 | 2.91 |
| attention (16 L) | 96 | 1,677,729,792 | 3.13 | 1.56 | 0.88 |
| `embed_tokens` | 1 | 1,271,398,400 | 2.37 | 1.18 | 0.67 |
| `lm_head` | 1 | 1,271,398,400 | 2.37 | 1.18 | 0.67 |
| vision tower | 333 | 460,730,096 | 0.86 | 0.43 | 0.24 |
| MTP head | 15 | 424,699,392 | 0.79 | 0.40 | 0.22 |
| norms / biases | 129 | 660,480 | 0.00 | 0.00 | 0.00 |
| **total** | **1199** | **27,781,427,952** | **51.75** | **25.87** | **14.55** |

Shapes confirm every contract claim that costs work:

- `q_proj [12288, 5120]` = 2 × 24 × 256 → `attn_output_gate`, Q output doubled.
- `o_proj [5120, 6144]` = 24 × 256 → the gate is consumed before `o_proj`.
- `in_proj_qkv [10240, 5120]` = 16·128 (Q) + 16·128 (K) + 48·128 (V) — the 3:1
  v:k head ratio is in the packing, not in separate tensors.
- `in_proj_z [6144, 5120]` = 48·128 → swish output gate, value-width.
- `conv1d [10240, 1, 4]` → depthwise causal conv over Q‖K‖V, kernel 4.
- MTP `mlp.{gate,up,down}_proj` → **dense** SwiGLU, not the MoE layout
  `mtp_head.h` documents, and it lives in the main shard index, not a sidecar.

## What already exists in this tree

| contract item | state | code path |
|---|---|---|
| arch registration `Qwen3_5ForConditionalGeneration` | ✅ | `hf_config_loader.cpp:37`, `model.cpp:326` |
| `layer_types[]`-driven stack (48 GDN + 16 full) | ✅ | `hf_config_loader.cpp:463-467`; log `GDN layers: 48 out of 64` |
| `attn_output_gate` | ✅ | `executor_attention.cu:110-144, 326, 570` — detected from `q_out_dim > nh*hd` |
| `partial_rotary_factor 0.25` | ✅ | log `Partial RoPE: 0.250 head_dim=256 → rope_dim=64` |
| M-RoPE interleaved `[11,11,10]` | ✅ | `hf_config_loader.cpp:214-238`, `mrope_positions.cpp` |
| GDN: 16 k-heads / 48 v-heads / conv 4 / swish | ✅ | log `GDN config: inner=6144 state=128 groups=16 n_heads=48 conv_kernel=4` |
| `mamba_ssm_dtype float32` never downcast | ✅ | `engine_init_resolver.cpp:450` — explicit GDN carve-out, with the shipped-bug note |
| dense MTP head loading | ✅ | `safetensors_loader.cpp:1035` `dense` branch |
| MTP in main index (no sidecar) | ✅ | `probe_mtp_head()` handles all three layouts |
| vision tower `model_type: qwen3_5` | ✅ | `qwen3vl_vision_config.cpp:46` |
| paged KV over the 16 attention layers only | ✅ | log `KV cache layers: 16 attention out of 64` |
| fixed recurrent-state slots | ✅ | `engine_kv_cache_init.cpp:702`, `SSMState::init` |
| CUDA graphs on GDN decode | ✅ | log `GDN model: CUDA graphs enabled (recurrent state in-place)` |
| chunked-parallel DeltaNet prefill | ✅ | `cfg::GDN::chunkwise_scan = true` by default |

`WeightMap (qwen35): assigned 1843 tensors, skipped 348` accounts fully:
348 = 333 vision (own loader) + 15 MTP (own loader). On the BF16 checkpoint
1199 = 851 LM + 333 vision + 15 MTP. **No tensor is missing and none unused.**

## Measured baseline

All on a free card (`gpu-busy-check.sh` FREE, 1016 MiB / 1 %), model
`Qwen3.8-27B-NVFP4` (self-quantized, 20 GB on disk), `imp:test` @ `72c642db`.

### Throughput, `imp-cli --bench --bench-pp 512 --bench-reps 3`

| arm | pp512 tok/s | tg128 tok/s |
|---|---:|---:|
| A baseline (`--bench` pins spec off) | 7425.67 | 88.60 |
| B `--set speculative.hybrid=true` (n-gram drafter, 79-81 % accept) | 7546.90 | 104.01 |
| C B + `--mtp-spec-decode 2` (MTP loaded, 90.7-91.4 % accept) | **1251.53** | **125.10** |

MTP buys **+41.2 % decode over A** and costs **-83 % prefill**. Both bench arms
ran with an identical KV pool (28 blocks / 896 tokens) and identical prefill
config, so the prefill collapse is not a memory artefact. RESOLVED 2026-08-25:
the feed forwarded the 425M-param head once per prompt token (M=1, ~800
us/token, length-independent 1250 tok/s). `mtp_feed_batch` (dense heads,
M=rows per chunk) restores pp512 to 6385-6510 vs 7070-7123 baseline (-8.8%),
accept unchanged at 90.5-93.3%. Item 8 below is closed; item 5 too
(`--set speculative.mtp_k` now works under imp-cli).

**Trap, hit during this measurement:** `--bench` pins `speculative.hybrid=false`
(`tools/imp-cli/main.cpp:88-95`), and Qwen3.8 is a hybrid, so measuring MTP the
obvious way silently measures nothing. Worse, `--set speculative.mtp_k=2` does
**not** enable MTP under `imp-cli` at all: the loader reads
`args.mtp_spec_decode_k` (`main.cpp:210`), fed only by the `--mtp-spec-decode`
flag. The startup log tells the operator to use `--set speculative.mtp_k=2`,
which is right for the server and wrong for the CLI.

### Memory, at `runtime.max_batch_size` = 1

| item | MiB | source |
|---|---:|---|
| card total | 32607 | `vram_allocator.cu:28` |
| CUDA context | 1680 | VRAM audit, `ctx_at_install` |
| library reserve (planned / measured) | 3900 / 3303 | `vram_budget.cpp:181`, `engine_workspace_warmup.cpp:442` |
| weight upload | 17920 | `engine_weight_upload.cpp:267` |
| CUTLASS NVFP4 SF cache | 1525.8 | pool table |
| KV pool (1815 blocks = 58080 tok) | 3630.0 | `engine_kv_cache_init.cpp:515` |
| shared workspace | 602.2 | `executor_workspace.cu:670` |
| SSM/GDN state, 1 seq | 151.5 | `ssm_state.cu:66` |
| engine arena (incl. vision 1106.6) | 1850.8 | `engine.cpp:746` |
| **used at init** | **29157** | **3450 free** |

### The two per-token/per-sequence constants

**KV cache: 65536 B/token (64 KiB), FP16.**
16 attention layers × 4 kv heads × 256 head_dim × 2 tensors × 2 B. Confirmed by
`engine_init_resolver.cpp:785` (`kv=65536 B/tok`). FP8 KV halves it to 32 KiB.

**DeltaNet state: 151.5 MiB/sequence.** Per layer: `h` = 48 v-heads × 128 × 128
× 4 B (FP32) = 3072 KiB, `conv` = 10240 ch × 4 × 4 B = 160 KiB. × 48 GDN layers
= 155,136 KiB. Confirmed verbatim by `ssm_state.cu:66`.

### What fits in 32 GB

Weights (NVFP4) 17920 MiB + context 1680 + library 3900 + arena 1851 + SF cache
1526 + workspace 602 leaves **≈ 5100 MiB** for KV *and* recurrent state.

| batch | GDN state | KV left | context/seq | verdict |
|---:|---:|---:|---:|---|
| 1 | 151.5 MiB | 58080 tok | 58080 | measured, healthy |
| 5 (auto default) | 757.5 MiB | 48512 tok | ~9700 | measured, the real operating point |
| 24 | 3636.0 MiB | **1376 tok** | **~57** | measured, loads but is unusable |
| 32 | 4848 MiB | — | — | **measured: does not load** |

At batch 32 the pre-upload reserve rises to 12715 MiB (from 7335 at batch 1),
leaving 16026 MiB for 17920 MiB of weights: the upload aborts at layer 61 with
`Failed to upload w_down for layer 61`. The recurrent state, not the KV cache,
is what makes 32-way concurrency infeasible on this card at NVFP4.

Auto-selected `max_seq_len` is 97204 (`vram_cap`), below the model's 262144.

### Serving correctness

32 concurrent `/v1/completions`, greedy, 4 prompts × 8, against the default
batch-5 server: **9/32 byte-identical to the batch=1 reference, 23 mismatches**.

That is not state leakage, and the data says so: for 3 of the 4 prompts all 8
concurrent answers are **byte-identical to each other** (prompt 2 gives 2
variants, and the odd one out is exactly the one that ran alone). Sequences do
not see each other's state; the batch>1 arithmetic path simply differs from
batch=1. imp's forward is not bit-deterministic across batch shapes generally —
this is not a Qwen3.8 property.

### Vision

`vision_test_green_bar.png` (green field, black horizontal bar) →
*"The image is green with a horizontal black stripe across the middle."*
Correct, including the detail the prompt did not ask for. Tower loads as 333
tensors / 27 blocks / 0 deepstack, 1106.6 MiB of arena.

## The four real gaps

### G1 — `reasoning_effort` is accepted and ignored (Phase 1)

The checkpoint's `chat_template.jinja:47-54` branches on `reasoning_effort`
(`xhigh` default, plus `medium`/`low`) and injects different
`reasoning_instructions`. imp implements the field only for gpt-oss/Harmony
(`tests/fixtures/gpt_oss_chat_template.jinja`); nothing reads it for Qwen.

Measured: `reasoning_effort: low` and `: xhigh` both yield **67 prompt_tokens**
and identical output. A working implementation must change the rendered prompt,
so `prompt_tokens` is the assertion.

Work: thread the field from the request through `ChatTemplate` into the Jinja
environment. `src/model/chat_template.cpp:93`, `src/model/jinja.cpp`,
`tools/imp-server/` request parsing. Golden test in `tests/refs/`.

### G2 — the FP8 checkpoint is a quantization source, not a loadable model (Phase 1)

`Qwen3.8-27B-FP8` uses a per-layer file layout (`layers-N.safetensors`, 51 KB
config) that `imp-quantize` reads (`tests/test_quantize_fp8_source.cpp`), but it
cannot be served: 25.87 GiB of weights against ~27 GiB of usable VRAM before KV,
and sm_120 has no FP8 GEMM, so there is no path that would be fast anyway.

Recommendation: **state this as a limitation, do not build it.** The brief asks
for "the FP8 checkpoint layout" under loading; loading it to then dequantize
everything to FP16 would need 51.75 GiB. `docs/LIMITATIONS.md` is the deliverable
here, not code.

### G3 — 32-way concurrency does not fit (Phase 3)

Quantified above. The lever is the 151.5 MiB/sequence recurrent state, and the
options, cheapest first:

1. **Admission control already exists** — the scheduler bounds concurrency
   (`vram_budget.cpp:534`). Serving 32 *clients* at batch 5 works today and is
   what the 24 s / 32-request run measured. If the brief's "32 concurrent"
   means throughput, this is already met.
2. **Recurrent-state paging.** `RecurrentSnapshotStore`
   (`engine_kv_cache_init.cpp:717`) already snapshots GDN state to a host-side
   budget for prefix caching. Extending it to evict *inactive* sequence slots
   would decouple slot count from batch size. This is the real fix and the
   largest piece of work in the whole plan.
3. **Smaller weights** (MXFP4 `lm_head`/`embed`) buy ~1 GiB = ~7 slots. Cheap,
   partial, and interacts with G4.

Never: FP16 recurrent state. `engine_init_resolver.cpp:450` records the shipped
bug that produced 37 % scan divergence, and the contract pins FP32.

### G4 — prefill CUDA-graph capture is off on this model (Phase 5)

```
gemm_nvfp4 dequant workspace: largest NVFP4 weight is 2425.00 MiB > 512 MiB cap
  — prefill graph capture disabled
```

2425 MiB is `lm_head` dequantized to FP16 (248320 × 5120 × 2 B = 2426.6 MiB).
One tensor's M>1 fallback size disables capture for the entire prefill path.
Fires on both bench arms, so it is unconditional for this checkpoint.

## What is genuinely absent, and honestly so

The brief's Phase 2 asks for logits parity against HF transformers (max abs diff
< 2e-2, top-5 order, 20 prompts at 4k/32k, plus a DeltaNet state-drift check at
32k). **No such test exists for this model, and I have not run one.** The
evidence that currently stands in for it is a teacher-forced perplexity of 4.62
on `ppl_corpus_45k.txt` (`docs/MODELS.md:55`) and coherent output. That is
weaker than the brief asks for and it is the one place where "first-class"
cannot be claimed from what is in the tree today.

Same for Phase 5's competitive table: no llama.cpp or vLLM numbers for this
model on this card exist in `docs/BENCHMARKS.md`.

## Plan, in dependency order

| # | work | files | acceptance |
|---|---|---|---|
| 1 | HF logits-parity harness for GDN hybrids | `tests/` + a pinned-transformers container | max abs diff < 2e-2, top-5 order, 20 prompts × {short, 4k, 32k} |
| 2 | DeltaNet state-drift check at 32k | same harness, compare `h` tensors not logits | state cosine ≥ 0.999 vs reference |
| 3 | `reasoning_effort` plumbing (G1) | `chat_template.cpp`, `jinja.cpp`, server request parse, `tests/refs/` | `prompt_tokens` differs across the three efforts; golden matches upstream template |
| 4 | FP8-as-source limitation documented (G2) | `docs/LIMITATIONS.md`, `docs/MODELS.md` | claim carries the 25.87 GiB figure and the sm_120 FP8-GEMM reason |
| 5 | ~~Fix the MTP enable-path mismatch~~ DONE 08-25 | `tools/imp-cli/main.cpp` | `--set speculative.mtp_k=2` enables MTP under the CLI |
| 6 | Recurrent-state paging (G3) | `RecurrentSnapshotStore`, `SSMState`, scheduler admission | 32 admitted sequences at a usable per-sequence context; no cross-sequence divergence |
| 7 | ~~Lift the prefill-capture cap (G4)~~ ANSWERED 08-25 | `engine_scheduler.cpp:1110` | the cap is not the binding gate: quantized KV append (D2H absmax sync) blocks capture on every non-F16-KV model, and capture covers only the offset-0 chunk by design. `diagnostics.prefill_graph_ignore_dequant_cap=true` A/B at pp4096: 7800.21 vs 7798.27 tok/s, same path both arms |
| 8 | ~~MTP prefill regression (Phase 5)~~ DONE 08-25 | `mtp_feed_batch`, `engine_spec_mtp.cpp` | pp512 with MTP -8.8% instead of -83% (residual: one M=1 chain pair per chunk) |
| 9 | ~~Competitive table~~ DONE 08-25 (vLLM half) | `docs/BENCHMARKS.md` | vLLM 0.27.1 vs imp at concurrency 1/8/32, same CT checkpoint, `[PROV:]`; llama.cpp half stays blocked (b10524 has no Qwen3_5 converter) |

Items 1-2 gate everything else: the brief's own rule is parity before
optimization, and items 6-8 all touch numerics.

## Phase 2 result — reference parity, measured

Harness built for this: `diagnostics.dump_final_logits_dir` (the post-soft-cap
LM logits as FP32 `.npy`, one file per forward pass) and
`diagnostics.dump_gdn_state_dir` (the recurrent state, plus a per-pass stats
line). Reference is `transformers 5.15.1`, which carries `qwen3_5` natively,
running the BF16 checkpoint on CPU (51.75 GiB in 74 GiB of host RAM).

### The acceptance criterion cannot be met, and not because of a defect

The brief asks for max abs logit diff < 2e-2 against HF **bf16, same weights**.
imp cannot hold this model's BF16 weights: 51.75 GiB against 32 GB of VRAM. What
imp serves is the NVFP4 quantization, so every comparison here is 4.5-bit
weights against 16-bit weights, and 2e-2 is below the quantization floor by
construction — not reachable by fixing anything.

Measured, 17 prompts (14 short, 3 × 5823 tokens), imp NVFP4 vs HF BF16:

| statistic | min | median | max |
|---|---:|---:|---:|
| max abs diff | 1.672 | 1.969 | 7.572 |
| mean abs diff | 0.248 | 0.283 | 1.060 |
| pearson r | 0.8256 | 0.9855 | 0.9904 |
| total variation (softmax) | 0.0080 | 0.1131 | 0.2793 |

top-1 token agrees on 14/17; top-5 as an exact ordering on 2/17; top-5 as a set
on 6/17. The reordering sits inside the quantization noise — on short01 imp has
`[264, 524]` at 14.733/14.691 where HF has them at 14.75/14.50, a 0.04 gap
against a 0.28 mean error.

### What settles that it is quantization and not a wrong implementation

Teacher-forced perplexity on `ppl_corpus_45k.txt`, identical token count
(13810 counted) under both:

| | mean NLL | PPL |
|---|---:|---:|
| HF transformers, BF16 | 1.4860 | **4.4194** |
| imp, NVFP4 | 1.5304 | **4.6202** |

**+4.54 %.** That is the price of 4-bit weights and nothing else: this repo's
own GGUF Q4_K_M path measured +8.4 % against a BF16 reference on the same kind
of comparison. A wrong `attn_output_gate`, a mis-sized partial RoPE or a broken
GDN scan does not land 4.5 % off — it lands orders of magnitude off, or
incoherent. The architecture is implemented correctly.

The honest limit of this evidence: it does not isolate imp's arithmetic from the
quantizer's. A BF16-vs-BF16 run would, and it needs either a card that fits
51.75 GiB or a smaller checkpoint of the same architecture.

### DeltaNet state after 46579 tokens

`ctx32k_1`, 46579 prompt tokens, 24 chunks of 2048 through the recurrent path,
48 GDN layers × 786432 FP32 elements each:

| | pass 0 (16 tok) | pass 2 (first full chunk) | pass 24 (last) |
|---|---:|---:|---:|
| RMS | 0.0257 | 0.0648 | 0.0728 |
| min | -10.69 | -49.52 | -44.92 |
| max | 30.37 | 36.74 | 112.2 |
| non-finite | 0 | 0 | 0 |

**0 non-finite values across all 26 passes.** RMS is flat from the first full
chunk onward (0.0625 to 0.0762 over 24 chunks, no trend); the max saturates
rather than growing without bound. The state does not drift, which is the
property `mamba_ssm_dtype: float32` exists to protect and the one a logits
comparison at the end of a long prefill cannot see.

### Long context: the prediction holds, the logit values drift

One 46579-token prompt (`ctx32k_1`), imp NVFP4 against the HF BF16 reference on
the same token ids:

| | short (median of 14) | 5823 tok (median of 3) | 46579 tok |
|---|---:|---:|---:|
| top-1 identical | 13/14 | 1/3 | **yes** |
| total variation | 0.0856 | 0.2114 | **0.0000** |
| pearson r | 0.9855 | 0.9738 | **0.7472** |
| max abs diff | 1.928 | 3.305 | **14.96** |
| mean abs diff | 0.279 | 0.455 | **2.509** |

Both halves of that are real and they are not in conflict. The distribution at
46579 tokens is extremely peaked — imp puts the top token at 35.53 against 23.06
for the runner-up — so after the softmax essentially all mass sits on one token
and both implementations pick it: total variation rounds to 0.0000, the
strongest agreement in the whole table. The *values*, meanwhile, drift: pearson
falls 0.986 → 0.747 and the mean absolute difference grows 9x. imp's whole logit
vector is scaled up relative to HF's (35.53 vs 29.50 on the same winning token),
and below the top the two orderings share nothing.

For what a user sees this is agreement. As a measurement it says the error
accumulates with context length, which is exactly what a 4-bit weight path
should do and exactly what a logits-only check at short context would miss.

Caveat on this row: it is ONE prompt, and its text is the 45k corpus repeated,
so it is more predictable than natural long text. Two more 46k prompts are
dumped on imp's side (`ctx32k_2`, `ctx32k_3`); their HF references were not run,
because each costs a CPU forward of that length.

### Still open in phase 2

- BF16-vs-BF16 isolation, as above: it needs a card that fits 51.75 GiB or a
  smaller checkpoint of the same architecture.
- Two of the three 46k prompts have no HF reference yet.

## Phase 3 result — 32-way concurrency, and a correction to G3 above

**G3 as written above is wrong.** It says 32-way concurrency "does not fit" and
calls recurrent-state paging "the real fix". Both readings came from measuring
only the DEFAULT configuration. With three settings changed it fits today, no
code required:

```
--set runtime.max_batch_size=32 --set kv_cache.dtype=fp8 \
--set runtime.max_seq_len=4096 --set runtime.deterministic=true \
--set server.prefix_cache=false
```

What was actually blocking the load is the pre-upload reserve, not the card. It
sizes a KV headroom from `max_seq_len × max_batch_size` and then clamps it to
`total_vram / 5` = 6521 MiB (`engine_weight_upload.cpp:180-195`) — a clamp that
does not move when KV goes to FP8, because the *uncapped* estimate is far above
it either way. Bounding `max_seq_len` puts the estimate under the clamp:

| config | reserve | left for weights (need 17920) | loads |
|---|---:|---:|---|
| default, bs=32 | 12715 | 16026 | no — aborts at layer 61 |
| FP8 KV, bs=32 | 12715 | 16026 | no — the clamp ignores dtype |
| FP8 KV + `max_seq_len=4096` | 10290 | 18451 | **yes** |

Resident at that point: weights 17920 MiB, GDN state 48 layers × **32
sequences** = 4848 MiB, KV 1343 blocks = **42976 tokens** FP8.

### Acceptance

32 concurrent `/v1/completions`, greedy, two prompt families interleaved (a
German-language instruction and a Python function body — their recurrent states
are shaped as differently as this corpus allows):

| | result |
|---|---|
| A identical to the batch=1 reference | **16/16** |
| B identical to the batch=1 reference | **16/16** |
| distinct answers within each family | 1 and 1 |
| A answers containing code tokens | 0 |
| B answers containing the German topic words | 0 |

State reset on completion, fresh server: 3 sequential requests cold, then one
32-way load, then 3 sequential again — **3/3 identical to the cold reference**.

### Both switches earn their place

Byte-identity is not free, and it is worth knowing which knob buys what:

| configuration | batch=32 identical to batch=1 |
|---|---|
| defaults (prefix cache on, non-deterministic) | 0/32, 2 variants |
| `server.prefix_cache=false` | 31/32 |
| `+ runtime.deterministic=true` | **32/32** |

So the prefix cache is what breaks byte-identity, not the batch path — the
opposite of what the phase-0 section above concluded from the default-config
run. Determinism closes the last sequence.

### The cost, stated plainly

`max_seq_len` drops 97204 → 4096, FP8 KV replaces FP16, the prefix cache is off
(so multi-turn re-prefills), and determinism pins GEMM algorithm selection. This
is a configuration for 32-way concurrency at short context, not a free upgrade.
Recurrent-state paging is still the lever that would give 32 sequences at long
context — it is just not what stands between this build and the phase-3
criterion.

## Phase 4 result — vision

Reference is `AutoModelForImageTextToText` + `Qwen3VLProcessor` from the same
transformers 5.15.1, BF16 on CPU, against imp's NVFP4 through
`/v1/chat/completions`.

### Preprocessing and splicing are exact

Both fixtures, both implementations, no tolerance involved:

| | imp | HF |
|---|---:|---:|
| prompt tokens | 124 | 124 |
| image placeholder tokens | 64 (8×8) | 64 |
| vision grid (t, h, w) | 16×16 patches | [1, 16, 16] |

imp additionally reports `position delta -56` for the M-RoPE offset. The tower
loads as 333 tensors / 27 blocks / 878.8 MiB, staying BF16 while the LM is
NVFP4 — that mixed precision is the checkpoint's, not a conversion.

### Logits: the decision matches, the distribution drifts more than text does

| fixture | top-1 | top-2 | pearson r | total variation |
|---|---|---|---:|---:|
| `vision_test_green_bar.png` | same | same | 0.667 | 0.318 |
| `vision_test_64.png` | same | same | 0.660 | 0.104 |
| **text-only control**, same template path, 58 tok | same | — | 0.656 | **0.0042** |

The text-only control is the row that makes this readable. Its pearson is the
same 0.66, so that figure is a property of comparing a full 248320-wide vector
whose tail is noise — not of the vision path. Total variation is the one that
separates them: 0.0042 with no image against 0.104-0.318 with one. The vision
path does contribute additional divergence, and top-1/top-2 survive it on both
fixtures. Captioning output is correct (a green field with a black bar is
described as exactly that).

### Text-only path unchanged

Perplexity on `ppl_corpus_45k.txt`, same 13810 counted tokens: **4.6124** now
against **4.6202** at phase 2, −0.17 %, inside run-to-run spread.

A trap on the way to that number, worth recording because it produced a
convincing false alarm: re-running the 14 phase-2 prompts and diffing the logit
dumps gave **0/14 bit-identical, max abs diff up to 22** — which reads as a
serious regression. It is not. Running the SAME build twice against itself gives
**1/14**. imp's forward is not bit-deterministic unless `runtime.deterministic`
is set, and neither dump had it, so the comparison could only ever measure
noise. An aggregate (perplexity) is the right instrument for that question;
a bit-compare needs both sides pinned.

### Video is absent by decision, and says so

`temporal_patch_size` is parsed but used only as a still-image repeat
(`docs/API.md:291`), and a `video_url` content part is **rejected at admission**
rather than silently dropped — with a CPU-lane test asserting the rejection
(`tests/test_constraint_validation.cpp:223`). So the brief's "image+video token
splicing" is half met on purpose: images splice, video refuses instead of
answering about a video the model never saw.

## Phase 5 result — the concurrency number is the finding

### Decode does not scale with concurrency, at all

`imp:test` from this tree, `Qwen3.8-27B-NVFP4`, `max_batch_size=32`, FP8 KV,
`max_seq_len=4096`, 200 tokens per request, median of 3 trials:

| concurrency | aggregate tok/s | per-stream tok/s | wall for the set |
|---:|---:|---:|---:|
| 1 | 84.23 | 84.23 | 2.4 s |
| 8 | 82.16 | 10.27 | 19.5 s |
| 32 | 80.35 | 2.51 | 79.7 s |

Aggregate throughput is flat. Thirty-two streams deliver what one stream
delivers.

That is not a client artifact, measured with a threaded urllib client that
records per-request start and finish: all 32 requests start within **0.02 s**,
**peak concurrent in flight is 32**, first finish 51.7 s, last 78.5 s, aggregate
81.51 tok/s. The server admits them all and runs them; the work simply does not
batch.

### It is the recurrent path, not this model

Same host, same harness, same 32×200 shape:

| model | kind | aggregate @32 | single-stream | scaling |
|---|---|---:|---:|---:|
| Qwen3-14B-NVFP4 | dense, no GDN | **1427.18** | ~168 | **8.5×** |
| Qwen3.6-35B-A3B-NVFP4 | MoE + GDN | 132.42 | 320 (BENCHMARKS.md) | **0.4×** |
| Qwen3.8-27B-NVFP4 | dense GDN | 81.51 | 84.23 | **1.0×** |

The dense non-GDN control scales 8.5×. Both GDN hybrids do not — and the MoE one
gets *slower* under load than it is single-stream. So this is a property of the
recurrent decode path in this engine, not of Qwen3.8, and it is the single
largest serving finding in this whole port.

Phase 3 stands unaffected: 32 sequences are admitted, isolated and byte-exact.
They are just not faster together than apart.

### Time to first token

Same build, prefix cache off, median of 3:

| context | prompt tokens | TTFT | prefill tok/s |
|---|---:|---:|---:|
| 4k | 5225 | **767 ms** | 6813 |
| 32k | 41680 | **7760 ms** | 5371 |
| 128k | — | not measurable | — |

128k does not fit: `max_model_len` resolves to 48512 tokens on this card, so the
brief's third context point is out of reach here, not slow.

### The competitive table, and what is missing from it

- **vs vLLM**: already in [`BENCHMARKS.md`](../BENCHMARKS.md) (2026-08-16) on
  the same NVFP4 file — imp 81.58 against vLLM 0.27.1 at 69.71 tok/s, +17 %,
  single stream. Today's 84.23 is consistent with that arm.
- **vs llama.cpp**: **not possible with the project's reference build.**
  b10524 (`9ee9fc04c`, the build BENCHMARKS.md's competitive sweep uses) has no
  `Qwen3_5` in `convert_hf_to_gguf.py`, so no GGUF of this checkpoint can be
  produced with it and there is nothing to serve. That is a support gap
  upstream, not a conversion cost — and it also means the existing llama.cpp
  comparison table cannot gain a Qwen3.8 row today.
- Concurrency 8/32 for vLLM was not measured. vLLM's design point is exactly
  the regime where imp is flat here, so that comparison is the interesting one
  and it is the honest hole in this phase.

## Context capacity — the 128k point is reachable, correcting phase 5

Phase 5 above says 128k is "unreachable here, not slow". That was measured at
the DEFAULT KV dtype and is wrong as a statement about the card.

The KV cache holds exactly what its name says: K and V for the 16 attention
layers only (the 48 GDN layers keep a fixed-size recurrent state instead). Per
token that is 16 layers x 4 kv heads x 256 head_dim x 2 tensors = 32768 values,
which is 64 KiB at FP16. The dtype of those values is a config key, and imp
already implements fp8, int8, int4, nvfp4 and mxfp4 for it.

Measured on this card, same model, nothing else changed:

| `kv_cache.dtype` | max_model_len | KV pool | perplexity | vs FP16 |
|---|---:|---:|---:|---:|
| fp16 (the default) | 48 512 | 3032 MiB | 4.6142 | — |
| fp8 | 96 960 | 3030 MiB | 4.6150 | **+0.02 %** |
| nvfp4 | 131 072 (auto cap) | 2304 MiB | 4.6385 | **+0.53 %** |
| nvfp4 + `max_seq_len=262144` | **172 032** | 3024 MiB | — | — |

**3.5x the context for half a percent of perplexity**, and FP8 doubles it for
what is indistinguishable from noise. 172 032 is where the card runs out; the
model itself declares 262 144.

Why the default is FP16 anyway: this family declares no `kv_cache_quant_algo`
hint, and `kv_fp8_hint_default_safe()` keeps the default conservative for
families whose long-context FP8 behaviour has not been verified. That is a
deliberate safety default, not an oversight — but an operator who wants context
should change it.

With `kv_cache.dtype=nvfp4`, TTFT across the brief's three context points:

| context | prompt tokens | TTFT | prefill tok/s |
|---|---:|---:|---:|
| 4k | 5 225 | 852 ms | 6 130 |
| 32k | 41 680 | 7 732 ms | 5 390 |
| **128k** | **123 822** | **34 380 ms** | **3 602** |

The 128k continuation is coherent text, not degenerate output.

One lever that did NOT work in this test: mounting
`vram.library_reserve_cache` is documented to replace the 3900 MiB planning
constant with the measured value (3311 MiB here, so ~590 MiB that could become
~33k more tokens). The mounted directory stayed empty across two starts and the
second start still planned with the constant. Not chased further; recorded so
the next person does not assume it is free capacity.

## Why GDN does not batch — the nsys profile

Profiled both models under identical load: 32 concurrent requests, 150 tokens
each, `nsys -t cuda --cuda-graph-trace=node`, capture window opened after the
weight upload. Kernel time share:

| kernel class | Qwen3.8-27B (GDN) | Qwen3-14B (dense) |
|---|---:|---:|
| CUTLASS GEMM (the batched path) | **1.0 %** | **71.8 %** |
| `gemv_nvfp4_*` + `gemv_fp16` (the M=1 path) | **~82 %** | ~7.4 % |
| paged attention | 3.7 % | 6.5 % |
| GDN scan + conv + gated norm | 3.8 % | — |

Exactly inverted. The instance counts say why: the GDN model issues **305 152**
`gemv_nvfp4_gate_up_fused_mr` launches for 4800 generated tokens across 64
layers — one per token per layer. The dense model issues **48 000** CUTLASS
launches for the same 4800 tokens, i.e. one per *batch step* per layer. One
model reads each weight once and serves 32 tokens with it; the other reads it 32
times. Decode is bandwidth-bound on that read, which is the whole 17.5x.

### The cause is one comment in the scheduler, and it is deliberate

`engine_scheduler.cpp:1503`:

> SSM/GDN: **the recurrent scan kernels are single-sequence**, so decode one
> sequence per step. […] Rotate the slice holder round-robin every
> `hybrid_decode_quantum` tokens instead; the decode graphs re-capture for the
> new sequence's state slot on rotation (~10-20 ms), which the quantum
> amortizes.

So concurrent GDN sequences are time-multiplexed, not batched — `runtime.
hybrid_decode_quantum` (default 128) is how long each holds the slice. That is
why phase 3's outputs are byte-exact and isolated while the aggregate is flat:
the engine really is running one sequence at a time, correctly.

### The lever, and its size

The scan that genuinely cannot batch is **3.8 %** of GPU time. It forces the
other **82 %** — the FFN and attention projections, which are ordinary GEMMs —
onto the M=1 path with it. A decode step that batched the projections across
sequences and dropped to per-sequence work only for the ~4 % recurrent section
would keep correctness (each sequence already owns its state slot) and address
the part of the profile that actually costs the time.

That is a real piece of engine work, not a config change: the step is currently
built around one sequence end to end, and the decode graphs are captured per
state slot. But it is now a measured target with a known ceiling rather than a
suspicion — and the ceiling is the dense model's scaling, 8.5x.

### After the fix — the profile inverted

Same 32-way load, same capture method, `imp:test` built with
`runtime.gdn_batched_decode` on:

| kernel class | before | after | dense reference |
|---|---:|---:|---:|
| CUTLASS GEMM | 1.0 % | **66.2 %** | 71.8 % |
| `gemv_*` (M=1) | ~82 % | **~1.5 %** | ~7.4 % |
| GDN scan | 2.4 % | **19.3 %** | — |
| conv1d decode | 0.9 % | 1.5 % |  — |

The GDN model's kernel mix now looks like the dense model's. Launch counts say
the same thing: the scan runs **21 024** times where it ran 230 400, i.e. once
per batch step per layer instead of once per token per layer.

Throughput in that run: 580.3 tok/s on the first wave of 32, **794.1 on the
second** once the card was warm — against 81.5 before.

### What the scan costs now, and the next lever

The scan went from 2.4 % to 19.3 % of GPU time, which is what happens when
everything around it gets ~8x faster. It is now the largest single non-GEMM
item, and it scales sublinearly: 5 929 ns per launch at (48 heads, 1 seq)
against 95 632 ns at (48, 32) — 32x the work for 16x the time, so batching it
already helped, but not proportionally.

The reason is in the kernel's own comment: each thread holds one column of the
state matrix in registers, `float H_reg[SS]` with SS=128. That is 128 floats per
thread and 64 KB of registers per block, which caps occupancy hard. A version
that stages the state through shared memory or splits the state dimension across
more threads would trade that register pressure for parallelism. Worth measuring
before building — the roofline pipeline (`make roofline-measure`) is the tool.

### SPLIT=4 and 8: measured, refuted

The obvious next step after SPLIT=2 was more of it. Register pressure and
occupancy both improve monotonically — and the kernel gets slower.

| | registers | threads/block | n_seq=16 | n_seq=32 | n_seq=64 |
|---|---:|---:|---:|---:|---:|
| SPLIT=1 | 255 + spill | 128 | 41.65 | 116.33 | 306.19 |
| **SPLIT=2** | **180** | **256** | **26.49** | **96.25** | **280.39** |
| SPLIT=4 | 114 | 512 | 40.40 | 100.28 | 286.14 |
| SPLIT=8 | 64 | 1024 | 58.15 | 109.67 | 293.16 |

Occupancy is not the binding constraint — the kernel is bandwidth-bound, and
SPLIT spends coalescing to buy occupancy. SPLIT threads share one state column,
so a warp touches 32/SPLIT distinct columns and reads 128/SPLIT bytes per
access: a full cache line at SPLIT=1, half at 2, an eighth at 8.

SPLIT=1 would be perfectly coalesced and spills instead (128 B spill stores).
SPLIT=2 is the one point where the spill is gone and the access is still half a
line. The table is in the kernel next to the constant so the next reader does
not repeat the experiment.

## MTP as a default: measured and declined (2026-08-25)

Matrix on the shipped build, server API, real prompt (200-token technical
answer), median of 3:

| concurrency | MTP off (tok/s aggregate) | MTP on |
|---:|---:|---:|
| 1 | 86.44 | 86.58 |
| 8 | 192.88 | 192.82 |
| 32 | 224.68 | 224.18 |

Identical because the uneconomic guard turns MTP off after the first
requests: real-text accept on this prompt is 38.2% (52 of 136 drafts),
below break-even at 19.6 ms/verify. What a default would still cost:
own_peak +800 MiB (26140 -> 26940), KV capacity 131072 -> 125024 tokens,
prefill -8.8% while the guard is still probing, and auto max_batch_size
13 instead of 28 (the head must fit after the recurrent state). MTP
stays opt-in: it pays on echo-heavy agent loops (+156% measured
elsewhere) and on anything with high self-similarity, not on free-form
prose.

### The acceptance gap, investigated to the wiring boundary

p1 (teacher-forced depth-1 accept) reads 70-78% across workloads against
the 87% a third party reports for the Qwen3-Next-80B-A3B head. Ruled out
this session, in addition to the 2026-08-17 list (RoPE, quantized head,
norm offsets, lm_head precision, head count):

- **pre- vs post-final-norm hidden as the head's input**: upstream taps
  the target's returned hidden (post `model.norm`), imp feeds the
  pre-norm residual. Extended `diagnostics.mtp_prenorm_h` to cover every
  fed pair (the old knob only covered the chain input, which is why its
  A/B read exactly 0). Measured off/on/on/off at k=1: 72.7 / 77.3 /
  75.8 / 78.5% accept - ranges overlap, no effect.
- **Wiring**: the miss list (`diagnostics.mtp_pattern_log`) is entirely
  near-miss alternatives at content-word branch points (' vectors' for
  ' tensors', ' allows' for ' supports', '.' for ','), zero trivial-token
  failures, no positional shift. A mis-fed head cannot reach the 91%
  accept the synthetic bench measures; this one does.

What remains is head quality (a 425M single-layer dense head against the
80B checkpoint's) and the verify price on a GDN hybrid (16 vs 11.6 ms
per step). Neither is an implementation defect.

## The concurrency gap, decomposed (2026-08-25, from the existing 32-way profile)

vLLM reads 1475 aggregate where imp reads 936 (same checkpoint, see
BENCHMARKS.md). From `gdn32_batched.sqlite` (438 decode steps, batch 32):

| item | per step | share |
|---|---:|---:|
| CUTLASS GemmUniversal (353 launches x 41.9 us) | 14.8 ms | 62.0 % |
| GDN scan (48 launches) | 4.6 ms | 19.3 % |
| rmsnorm (119 launches) | 0.65 ms | 2.7 % |
| activation quantize (292 launches) | 0.35 ms | 1.5 % |
| everything else | ~3.4 ms | ~14 % |

The GEMM class runs at ~54 % of the weight-bandwidth floor (~8 ms for the
NVFP4 weights at 1792 GB/s), and the per-CTA-count breakdown says why —
grid starvation on 170 SMs:

| CTAs/launch | launches | total ms | avg us | vs floor |
|---:|---:|---:|---:|---|
| 136 | 53 760 | 2878 | 53.5 | ~49 % (44.6 MB B-read, 26 us floor) |
| 40 | 53 760 | 2224 | 41.4 | **19 %** (14 MB B-read, 7.8 us floor) |
| 80 | 20 160 | 652 | 32.3 | — |
| 48 | 20 160 | 456 | 22.6 | — |

The 40-CTA class is the N=5120 shapes (o_proj / fc / down) under the
128x128x128 tile: M=32 gives one M-tile row, so the grid is N/128 = 40
CTAs — 24 % of the SMs. Same disease the small-N pingpong variant
(#596, 2.1x) already cured for N<=2048, now on the M side. First lever measured and REFUTED
(2026-08-25): routing M<=64 onto the existing 128x64 pingpong variant
doubles the grid (40 -> 80 CTAs) and still loses — aggregate 358 -> 249
at 8 streams (-30%) and 936 -> 792 at 32 (-15%); the 64-wide N-tile's
per-CTA efficiency loss outweighs the extra CTAs, matching the #596
"loses ~25% on large N" note. Two more levers measured
and REFUTED the same night: (b) a 64-row M-tile instance does not
build — the sm120 block-scaled SF atom is 128-row, CUTLASS static-asserts
on any CTA_M below 128; (c) swapping the StreamKScheduler into the
128x128 kernel builds and passes correctness, and collapses under load:
181.8 tok/s aggregate at 32 streams against 935.7 baseline (283 vs 358
at 8) — with 353 GEMM launches per step, stream-K's per-launch workspace
fixup dwarfs what the extra parallelism buys. What remains is a properly
configured stream-K (hw_info, DataParallel decomposition, graph-safe
workspace) or a dedicated M<=32 weight-streaming kernel; both are real
campaigns, not dispatch flips. Expected: GEMM 14.8 ->
~9-10 ms/step, aggregate ~1050+ against vLLM's 1475. The scan is near
its own state-bandwidth floor (9.7 GB/step at 32 seqs); the 292
per-step activation-quantize launches are the secondary item.

## Notes for whoever picks this up

- The GPU must be checked free before every job, not once
  (`~/.claude/skills/gpu-stats/gpu-busy-check.sh`), and peer sessions told.
- `imp:test` is the `make build` image; `make dev` does not rebuild it.
- Reproduce the concurrency numbers with `runtime.max_batch_size` pinned — free
  VRAM before the weight upload swings by ~1.6 GB between runs.
