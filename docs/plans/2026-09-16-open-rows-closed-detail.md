# Open rows closed (moved out of docs/roadmap.md on 2026-09-16)

Record. The rows below are the verdict text of the `## Open` items that had
already closed by 2026-09-16 (1, 2, 4, 7, 13, 14, 15); `docs/roadmap.md`
keeps one Closed row each with the headline number and the ref, and links here.

Text is verbatim as of `docs/roadmap.md` at commit `cf96f768`. `path:line`
citations inside are as they stood that day and are not gated in this file.

| # | item | state | ref |
|---|---|---|---|
| 1 | launch-coupled idle @32 | RE-PRICED 2026-09-06, the headroom is ~2%, not ~8%: idle 7.2% of the wall over 873 decode steps, of which the sub-10-us gaps (every launch in the step) are 2.2%, host moments 10 us-1 ms 3.8%. A step carries 1478 device intervals (1296 kernels + 182 memcpy). Launch count and device time rank differently: `nvjet`+`splitKreduce` are 13.2% of the launches for 2.2% of the step, `[memcpy]` 12.3% for 1.8%, `swiglu`+`quantize`+`device_copy` 13.0% for 1.5%, while `gemm_nvfp4_smallm_v2`+`_pair` are 19.6% for 63%. Removing launches that overlap inside the graph buys nothing: #1793 took 65/step out and measured -0.9%. A fusion is worth building here only if it also removes traffic or math | `tools/analysis/nsys_gap_attribution.py --window` (launch census) |
| 2 | paced serving prefill, dense | CLOSED (2026-09-08). Anatomy from a debug-log step timeline (Qwen3-14B-NVFP4, 32 x 982-token prompts, wave 2): each capped step prefilled 1024 rows in 56-59 ms against a 38 ms standalone 1024-row forward (26.6k tok/s; 2048 rows 27.9k, 4096 rows 26.4k), the rest was the decode step and host turnaround the step carried; 29 such steps = 1.68 s ingest. SHIPPED in three parts: `runtime.prefill_cap_fairness` (the cap scales by W x waiting / decoding; W=1 22 steps = 1.52 s, +3.0..+4.2% aggregate; W=4 default a further +1.9..+2.5%), then `runtime.prefill_mixed_decode` (the decoders ride the ragged prefill forward as one-row members with one batched paged-decode attention launch per layer, no separate decode step): the 2048-row step 86 -> 65 ms, ingest 1.455 -> 1.175 s against vLLM's ~1.1 s, aggregate +6.6..+7.8%, TTFT p50 -20..-23%, ITL max 141 -> 80 ms, gaps over 100 ms 22 -> 0. What is left is the prefill forward itself (26-28k rows/s at 2048 rows, 80% of the FP4 peak) | ledger 2026-09-08, #1950, #1951, #1952 |
| 4 | speculation adapts per request | CLOSED 2026-09-15. Chain depth adapts (#1801), `mtp_k=auto` drafts single-stream 95.8 -> 141.6 tok/s (+48%). The "drafter choice is global" half has no target: on Qwen3.8-27B-NVFP4 single-stream greedy 300 tokens (3 repeats each) MTP reads code 140.4 / 140.2, prose 97.6 / 97.4, list 101.5 / 102.1 tok/s against no drafter 97.7 / 97.7, 97.8 / 97.9, 97.9 / 97.8, so it wins +43% where the text repeats and costs nothing where it does not (the acceptance give-up makes it free); n-gram alone never leaves the no-drafter line (97.5 / 98.6, 96.5 / 96.3, 96.3 / 96.9, acceptance 11.6%). Per-request on/off and depth already exist (`speculative` field). The chain saturates near 2.5 accepted/verify and the multi-candidate tree measured no gain past it | #1809, #1811, [plan](2026-08-31-mtp-multicandidate-hybrid.md) |
| 7 | quantizer refuses 3-D stacked experts | CLOSED 2026-09-16 for gpt-oss (Gemma-4 unrun): gpt-oss-20b BF16 -> NVFP4 in one `imp-quantize` run (2400 tensors, 12 820 MiB), loads, `ppl_corpus_45k` PPL 179.23 vs 312.50 for the MXFP4 GGUF in the same binary (gpt-oss restart class 151-308), greedy Harmony coherent, verify-fast + paired A/B neutral (decode -0.04 %, prefill +0.30 %). Built 2026-09-15: `imp-quantize` splits a stack into the per-expert 2-D matrices by a per-model_type descriptor (`expert_destack.cpp`: gpt-oss [ne, K, N] interleaved, Gemma-4 [ne, N, K] concatenated; a model_type without one is refused), gate/up of one expert share a tensor scale, the biases pass through to the loader's existing de-interleave. Loader side: gpt-oss's 2^-4 residual rescale refused every non-BF16 Wo, now an NVFP4 Wo and the NVFP4 expert down projections carry it in `tensor_scale` at Phase 0. Per-expert bias and the MoE forward needed nothing: both paths add `expert_*_bias` under `is_gpt_oss` already. Open: no BF16 gpt-oss / Gemma-4 source on the host, so quantize + load + PPL are unrun; `test_quantize_expert_destack.cpp` pins the split on the real shapes | finding (f) |
| 13 | batched decode is W4A4, single-stream is W4A16 | MEASURED 2026-09-15, unpriced: every 2..32-row NVFP4 GEMM packs its activations to NVFP4 (both small-M impls are `_a4`), the M=1 GEMV reads FP16 activations. Qwen3.8-27B-NVFP4: layer-0 GDN in-proj 0.0284 relL2 off the dequantized-weight reference (solo 0.0002), residual 0.14 by layer 62, first-decode-step logit delta 0.36-1.09 nats vs solo; `gemm.nvfp4_smallm_a4=false` plus an FP16-activation LM head brings it to 0.015-0.039 (the Qwen3.5-4B GGUF class). Not the #2019 loop (that was the sampler). PRICED AND CLOSED 2026-09-15: A16 everywhere costs 38% at 32 streams (wave medians 1899.7 / 1947.2 / 1903.4 vs 1186.7 / 1172.1 / 1172.8 tok/s, 3 alternating pairs on one binary); A16 on the state-feeding GDN in/z/out projections only leaves the delta at 0.45-1.05 nats (0.35-0.93 with the FP16 LM head), so attention and FFN GEMMs carry it and no cheap subset exists. The remaining lever is a different activation scheme, not a kernel switch and not the block scale: an emulation of the quantizer (per-16 block, e4m3 scale nearest, e2m1 nearest) reproduces the batched layer-0 output to 0.0042 relL2, and an exact fp32 block scale lowers the 0.0285 error only to 0.0280 (round-up 0.0296); the e2m1 element grid (per-element 0.094) is the floor. The LM head alone on FP16 activations (`nvfp4_lm_head_smallm=false nvfp4_lm_head_cutlass=false`, GEMMs A4) is dead too: delta 0.40-1.02 nats (no gain) at 1999.4 / 1980.4 / 2009.4 -> 1576.9 / 1576.3 / 1580.9 tok/s (-21%) | #2026 |
| 14 | hybrid mixed prefill+decode step | BUILT AND REFUTED 2026-09-15: the rider geometry pushed recurrent slot 0 (another sequence's), which is what killed the first attempt (illegal memory access; the `head_dim=256` refusal was the sticky error read back). With the rider's live slot the step runs (`runtime.prefill_mixed_decode_hybrid`, opt-in): correctness in the rounding class (A16 numerics, first divergence from solo per row 43/18/32/6/32/32/32/4 off vs 43/18/32/6/28/19/32/21 on), throughput NEGATIVE on Qwen3.8-27B-NVFP4, 32 x 1k-token prompts x 300 tokens, 3 alternating pairs: 868.6 / 869.7 / 874.1 off vs 840.6 / 822.5 / 831.5 on (-4.6%). The rider rows take the ragged GDN chunk path instead of the batched decode kernel, and the separate decode step is the cheaper one. ITL arm negative too (31 short streams x 300 tokens, one 4400-token prompt 2 s into the wave, 3 pairs): the short streams' ITL max during the ingest 181-184 ms on vs 126-128 ms off, the late stream's TTFT 836-863 vs 649-652 ms. Every ingest step carries the rider rows through the ragged GDN chunk path. CLOSED; the knob stays as the record | #2032 |
| 15 | multi-turn prefix reuse breaks on non-canonical BPE merges | CLOSED 2026-09-15 (`server.transcript_token_reuse`, default on). A generated reply re-tokenized differently where the model emitted a non-canonical split: Qwen3.8-27B wrote ` pre` + `pref` (ids 829 29554), the tokenizer re-encodes ` prepref` as ` prep` + `ref` (20671 1062), so the next turn's hash chain broke at that block (`lookup chain breaks at block 13 of 80`) and the whole reply re-prefilled (`cached_tokens` 0). Lever shipped: every finished reply's ids (prompt + reply as forwarded, trailing stop ids dropped) sit in a 256-entry store on the chat template; a render whose text starts with a stored transcript takes those ids and tokenizes only the tail (`TranscriptIdStore::splice`). Two publication races closed on the way: the store is filled by the server's delivery loop before the finish reaches the client (an engine-side remember at the deferred release ran 12 ms after the response and the seam came back once in 32 sessions), and a pipelined row's final tokens are held until the drain published its KV hashes and transcript snapshot (`Request::release_pending`; before, 5 of 8 sessions in one run read `no restorable prefix` with 19/19 to 80/80 blocks cached). `think_loop_probe.py NOLP=1`, 8 sessions x 6 runs: 48 of 48 turn-2 renders spliced, 40 of 40 sessions cached > 0 in the 5 runs that completed, no chain break inside a reply; the sixth run hit the dev pool's 1366-block VRAM clamp (one 503, 5 restore misses at full chains): capacity, not the seam. `TranscriptIdStoreTest.ResentReplyKeepsTheModelSplit` | #2028 |
