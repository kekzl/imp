=== sampling files ===
   250 src/compute/sampling.cu
   213 src/compute/sampling_filters.cu
   541 src/compute/sampling_penalties.cu
   764 src/compute/sampling_topk_topp.cu
   162 src/compute/sampling.h
   493 src/runtime/engine_sampling_stop.cpp
  2423 total

=== sampling entry points ===
src/compute/sampling_topk_topp.cu:683:int32_t sample_topk_topp(const Tensor& logits, int top_k, float top_p, float temperature, unsigned int seed,
src/compute/sampling_topk_topp.cu:704:int32_t sample_topk_topp(const Tensor& logits, int top_k, float top_p, float temperature, unsigned int seed,
src/compute/sampling_topk_topp.cu:738:void sample_topk_topp_device(const Tensor& logits, int top_k, float top_p, float temperature,
src/compute/sampling.cu:149:int32_t sample_greedy(const Tensor& logits, cudaStream_t stream) {
src/compute/sampling.cu:186:int32_t sample_greedy(const Tensor& logits, int32_t* d_result, cudaStream_t stream) {
src/compute/sampling.cu:209:void sample_greedy_async(const Tensor& logits, int32_t* d_result, cudaStream_t stream) {
src/compute/sampling.cu:231:void sample_greedy_device(const Tensor& logits, int32_t* d_result, int32_t* h_mapped, cudaStream_t stream) {
src/compute/sampling_penalties.cu:417:int32_t sample_mirostat_v2(const Tensor& logits, float temperature, float tau, float eta, float* mu,
src/compute/sampling_penalties.cu:428:int32_t sample_mirostat_v2(const Tensor& logits, float temperature, float tau, float eta, float* mu,

=== apply_mask / constrained bypass ===
src/exec/executor.cu:59:        st.grammar_constrainer->apply_mask(logits, vocab, stream);
src/exec/executor.cu:61:        st.regex_constrainer->apply_mask(logits, vocab, stream);
src/exec/executor.cu:63:        st.schema_constrainer->apply_mask(logits, vocab, stream);
src/exec/executor.cu:69:        st.json_constrainer->apply_mask(logits, vocab, stream);
